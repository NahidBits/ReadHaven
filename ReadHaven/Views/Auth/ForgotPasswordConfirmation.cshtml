@{
    ViewData["Title"] = "Password Reset Link Sent";
}

<div class="container">
    <div class="row justify-content-center">
        <div class="col-md-6">
            <div class="card">
                <div class="card-header text-center">
                    <h4>Password Reset</h4>
                </div>
                <div class="card-body">
                    <div class="alert alert-success" role="alert">
                        A password reset link has been sent to your email address.
                    </div>
                    <p>If you do not see the email in your inbox, please check your spam folder.</p>
                    <p>Follow the link in the email to reset your password.</p>
                    <div class="text-center">
                        <a href="@Url.Action("Index", "Auth")" class="btn btn-primary">Back to Login</a>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

